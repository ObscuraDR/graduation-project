[1mdiff --git a/.env.example b/.env.example[m
[1mindex d6c94d9..d062c89 100644[m
[1m--- a/.env.example[m
[1m+++ b/.env.example[m
[36m@@ -3,30 +3,13 @@[m
 # production   → strict validation; startup fails on weak keys or missing CORS[m
 ENVIRONMENT=development[m
 [m
[31m-# Database[m
[32m+[m[32m# PostgreSQL Database[m
 POSTGRES_HOST=localhost[m
 POSTGRES_PORT=5432[m
 POSTGRES_DB=ids_db[m
 POSTGRES_USER=ids_user[m
 POSTGRES_PASSWORD=ids_password[m
 [m
[31m-# MongoDB[m
[31m-MONGODB_HOST=localhost[m
[31m-MONGODB_PORT=27017[m
[31m-MONGODB_DB=ids_logs[m
[31m-# URI-style override (takes priority over host/port fields when set)[m
[31m-# Docker: mongodb://ids_mongo_user:ids_mongo_pass@localhost:27017/ids_logs?authSource=admin[m
[31m-MONGO_URI=[m
[31m-MONGO_DB=ids_logs[m
[31m-[m
[31m-# Redis[m
[31m-REDIS_HOST=localhost[m
[31m-REDIS_PORT=6379[m
[31m-REDIS_DB=0[m
[31m-# URI-style override (takes priority over host/port fields when set)[m
[31m-# Docker: redis://:ids_redis_pass@localhost:6379/0[m
[31m-REDIS_URL=[m
[31m-[m
 # API[m
 API_HOST=0.0.0.0[m
 API_PORT=8000[m
[1mdiff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml[m
[1mindex e6c555a..65f05d8 100644[m
[1m--- a/.github/workflows/ci.yml[m
[1m+++ b/.github/workflows/ci.yml[m
[36m@@ -29,25 +29,6 @@[m [mjobs:[m
           --health-timeout 5s[m
           --health-retries 5[m
 [m
[31m-      mongodb:[m
[31m-        image: mongo:6[m
[31m-        ports:[m
[31m-          - 27017:27017[m
[31m-        options: >-[m
[31m-          --health-cmd "mongosh --eval 'db.runCommand({ping: 1})'"[m
[31m-          --health-interval 10s[m
[31m-          --health-timeout 5s[m
[31m-          --health-retries 5[m
[31m-[m
[31m-      redis:[m
[31m-        image: redis:7[m
[31m-        ports:[m
[31m-          - 6379:6379[m
[31m-        options: >-[m
[31m-          --health-cmd "redis-cli ping"[m
[31m-          --health-interval 10s[m
[31m-          --health-timeout 5s[m
[31m-          --health-retries 5[m
 [m
     steps:[m
       - uses: actions/checkout@v4[m
[36m@@ -88,11 +69,6 @@[m [mjobs:[m
           POSTGRES_DB: ids_db[m
           POSTGRES_USER: ids_user[m
           POSTGRES_PASSWORD: ids_password[m
[31m-          MONGODB_HOST: localhost[m
[31m-          MONGODB_PORT: 27017[m
[31m-          MONGODB_DB: ids_logs[m
[31m-          REDIS_HOST: localhost[m
[31m-          REDIS_PORT: 6379[m
           API_KEY: test-api-key-for-ci-do-not-use[m
           SECRET_KEY: test-secret-key-for-ci-only-do-not-use-in-production[m
           CORS_ORIGINS: http://localhost:3000[m
[1mdiff --git a/README.md b/README.md[m
[1mindex 06021e1..81d30b9 100644[m
[1m--- a/README.md[m
[1m+++ b/README.md[m
[36m@@ -7,24 +7,31 @@[m [mHệ thống phát hiện xâm nhập mạng thời gian thực dựa trên Mach[m
 ## Quick Start[m
 [m
 ```bash[m
[31m-# 1. Clone & cài dependencies[m
[32m+[m[32m# 1. Clone & cài core dependencies[m
 pip install -r requirements.txt[m
 cp .env.example .env[m
 [m
[31m-# 2. Tạo dummy models (cho dev/demo)[m
[32m+[m[32m# 2. (Tùy chọn) Cài thêm package cho ML training / email / load test[m
[32m+[m[32mpip install -r requirements-optional.txt[m
[32m+[m[32m# Hoặc cài toàn bộ môi trường dev + feature:[m
[32m+[m[32m# pip install -r requirements-all.txt[m
[32m+[m
[32m+[m[32m# 3. Tạo dummy models (cho dev/demo)[m
 python backend/ml/create_dummy_models.py[m
 [m
[31m-# 3. Start databases + backend[m
[32m+[m[32m# 4. Start databases + backend[m
 docker compose up -d postgres[m
 uvicorn backend.main:app --host 0.0.0.0 --port 8000[m
 [m
[31m-# 4. Start frontend (terminal khác)[m
[32m+[m[32m# 5. Start frontend (terminal khác)[m
 cd frontend && npm install && npm run dev[m
 [m
[31m-# 5. Mở dashboard[m
[32m+[m[32m# 6. Mở dashboard[m
 # http://localhost:3000[m
 ```[m
 [m
[32m+[m[32m```[m
[32m+[m
 ---[m
 [m
 ## Cấu trúc dự án[m
[36m@@ -43,8 +50,8 @@[m [mz-sentinel-ids/[m
 │   ├── detection_engine/          — ML inference + model loading[m
 │   ├── alert_engine/              — Alert generation & correlation[m
 │   ├── api/                       — HTTP routes, WebSocket, middleware[m
[31m-│   ├── database/                  — PostgreSQL, MongoDB, Redis clients[m
[31m-│   ├── cache/                     — Redis cache wrapper[m
[32m+[m[32m│   ├── database/                  — PostgreSQL client[m
[32m+[m[32m│   ├── cache/                     — In-memory cache wrapper[m
 │   ├── notifications/             — Email service (SMTP)[m
 │   ├── monitoring/                — Prometheus metrics[m
 │   ├── ml/                        — ML training & XAI scripts[m
[36m@@ -75,9 +82,11 @@[m [mz-sentinel-ids/[m
 │[m
 ├── logs/                          ← Runtime logs (gitignored)[m
 │[m
[31m-├── docker-compose.yml             — PostgreSQL + MongoDB + Redis + Backend + Dashboard + Nginx[m
[32m+[m[32m├── docker-compose.yml             — PostgreSQL + Backend + Dashboard + Nginx[m
 ├── Dockerfile                     — Backend image[m
[31m-├── requirements.txt               — Python dependencies[m
[32m+[m[32m├── requirements.txt               — Core Python dependencies[m
[32m+[m[32m├── requirements-optional.txt      — Optional features / ML / integrations[m
[32m+[m[32m├── requirements-dev.txt           — Development & test dependencies[m
 ├── pytest.ini                     — Test configuration[m
 ├── .env.example                   — Environment template[m
 ├── .gitignore[m
[36m@@ -112,7 +121,8 @@[m [mz-sentinel-ids/[m
 | Backend | FastAPI 0.104 + Uvicorn (Python 3.10+) |[m
 | Frontend | React 18 + Vite + TailwindCSS + Recharts |[m
 | ML | scikit-learn, XGBoost, SHAP |[m
[31m-| Databases | PostgreSQL 14, MongoDB 6, Redis 7 |[m
[32m+[m[32m| Database | PostgreSQL 14 |[m
[32m+[m[32m| Cache | In-memory (Python dict with TTL) |[m
 | Packet Capture | Scapy (Npcap on Windows / libpcap on Linux) |[m
 | Deployment | Docker Compose |[m
 [m
[1mdiff --git a/backend/alembic/versions/001_initial_schema.py b/backend/alembic/versions/001_initial_schema.py[m
[1mindex ce04a7b..2bfc5f9 100644[m
[1m--- a/backend/alembic/versions/001_initial_schema.py[m
[1m+++ b/backend/alembic/versions/001_initial_schema.py[m
[36m@@ -193,6 +193,27 @@[m [mdef upgrade() -> None:[m
     )[m
     op.create_index("ix_whitelist_ip_address", "whitelist", ["ip_address"])[m
 [m
[32m+[m[32m    # flow_logs (replaces MongoDB flow_logs)[m
[32m+[m[32m    op.create_table([m
[32m+[m[32m        "flow_logs",[m
[32m+[m[32m        sa.Column("id", sa.Integer(), nullable=False),[m
[32m+[m[32m        sa.Column("flow_id", sa.Integer(), nullable=True),[m
[32m+[m[32m        sa.Column("src_ip", sa.String(45), nullable=False),[m
[32m+[m[32m        sa.Column("dst_ip", sa.String(45), nullable=False),[m
[32m+[m[32m        sa.Column("src_port", sa.Integer(), nullable=True),[m
[32m+[m[32m        sa.Column("dst_port", sa.Integer(), nullable=True),[m
[32m+[m[32m        sa.Column("protocol", sa.String(10), nullable=False),[m
[32m+[m[32m        sa.Column("features", postgresql.JSON(), nullable=True),[m
[32m+[m[32m        sa.Column("timestamp", sa.DateTime(), nullable=True),[m
[32m+[m[32m        sa.Column("created_at", sa.DateTime(), nullable=True),[m
[32m+[m[32m        sa.ForeignKeyConstraint(["flow_id"], ["traffic_flows.id"]),[m
[32m+[m[32m        sa.PrimaryKeyConstraint("id"),[m
[32m+[m[32m    )[m
[32m+[m[32m    op.create_index("ix_flow_logs_src_ip", "flow_logs", ["src_ip"])[m
[32m+[m[32m    op.create_index("ix_flow_logs_dst_ip", "flow_logs", ["dst_ip"])[m
[32m+[m[32m    op.create_index("ix_flow_logs_timestamp", "flow_logs", ["timestamp"])[m
[32m+[m[32m    op.create_index("ix_flow_logs_created_at", "flow_logs", ["created_at"])[m
[32m+[m
     # metrics[m
     op.create_table([m
         "metrics",[m
[1mdiff --git a/backend/alert_engine/alert_manager.py b/backend/alert_engine/alert_manager.py[m
[1mindex 879c58b..66e9b7c 100644[m
[1m--- a/backend/alert_engine/alert_manager.py[m
[1m+++ b/backend/alert_engine/alert_manager.py[m
[36m@@ -286,19 +286,27 @@[m [mclass AlertManager:[m
             )[m
     [m
     def _is_in_cooldown(self, ip_address: str) -> bool:[m
[31m-        """Check cooldown via Redis (falls back to in-memory if Redis unavailable)."""[m
[32m+[m[32m        """Check cooldown using local history and the shared cache."""[m
[32m+[m[32m        # If cooldown disabled (0 or negative), never consider in cooldown[m
[32m+[m[32m        if self.alert_cooldown <= 0:[m
[32m+[m[32m            return False[m
[32m+[m
[32m+[m[32m        # Prefer in-memory manager-local history (tests create fresh managers)[m
[32m+[m[32m        if ip_address in self.alert_history:[m
[32m+[m[32m            return datetime.utcnow() < self.alert_history[ip_address] + timedelta(seconds=self.alert_cooldown)[m
[32m+[m
[32m+[m[32m        # Otherwise consult the shared cache[m
         cache = get_cache()[m
         if cache.is_connected():[m
             return cache.is_alert_in_cooldown(ip_address)[m
[31m-        # in-memory fallback[m
[31m-        if ip_address not in self.alert_history:[m
[31m-            return False[m
[31m-        return datetime.utcnow() < self.alert_history[ip_address] + timedelta(seconds=self.alert_cooldown)[m
[32m+[m
[32m+[m[32m        return False[m
 [m
     def _update_alert_history(self, ip_address: str):[m
[31m-        """Record cooldown in Redis (and in-memory as fallback)."""[m
[32m+[m[32m        """Record cooldown in the shared cache when available."""[m
         cache = get_cache()[m
[31m-        if cache.is_connected():[m
[32m+[m[32m        # Only set cooldown if enabled (positive ttl)[m
[32m+[m[32m        if self.alert_cooldown > 0 and cache.is_connected():[m
             cache.set_alert_cooldown(ip_address, self.alert_cooldown)[m
         self.alert_history[ip_address] = datetime.utcnow()[m
     [m
[1mdiff --git a/backend/api/auth.py b/backend/api/auth.py[m
[1mindex c8ab8fc..6da929b 100644[m
[1m--- a/backend/api/auth.py[m
[1m+++ b/backend/api/auth.py[m
[36m@@ -183,10 +183,14 @@[m [mdef authenticate_user(db: Session, username: str, password: str) -> Optional[Use[m
     """Return the User on valid credentials, else None."""[m
     user = UserRepository.get_by_username(db, username)[m
     if user is None:[m
[32m+[m[32m        logger.warning("Auth failure: User %r not found in database", username)[m
         verify_password(password, "$2b$12$" + "x" * 53)[m
         return None[m
[32m+[m[41m    [m
     if not verify_password(password, user.password_hash):[m
[32m+[m[32m        logger.warning("Auth failure: Password mismatch for user %r", username)[m
         return None[m
[32m+[m[41m        [m
     return user[m
 [m
 [m
[36m@@ -231,6 +235,24 @@[m [masync def verify_csrf_token(request: Request):[m
 [m
 [m
 # ── Routes ───────────────────────────────────────────────────────────────────[m
[32m+[m[32m@auth_router.post("/register", response_model=UserInfo, status_code=status.HTTP_201_CREATED)[m
[32m+[m[32masync def register_public([m
[32m+[m[32m    user_data: UserCreateRequest,[m
[32m+[m[32m    db: Session = Depends(get_db)[m
[32m+[m[32m):[m
[32m+[m[32m    """[m
[32m+[m[32m    Endpoint đăng ký công khai phục vụ mục đích testing/khởi tạo.[m
[32m+[m[32m    Lưu ý: Nên đóng endpoint này hoặc giới hạn quyền trong môi trường production.[m
[32m+[m[32m    """[m
[32m+[m[32m    if UserRepository.get_by_username(db, user_data.username):[m
[32m+[m[32m        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Tên đăng nhập đã tồn tại")[m
[32m+[m[41m    [m
[32m+[m[32m    if UserRepository.get_by_email(db, user_data.email):[m
[32m+[m[32m        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Email đã tồn tại")[m
[32m+[m
[32m+[m[32m    new_user = UserRepository.create(db, user_data.model_dump())[m
[32m+[m[32m    return UserInfo(id=new_user.id, username=new_user.username, email=new_user.email, role=new_user.role)[m
[32m+[m
 @auth_router.post("/login", response_model=TokenResponse)[m
 async def login(response: Response, request: Request, payload: LoginRequest, db: Session = Depends(get_db)):[m
     """[m
[36m@@ -243,7 +265,7 @@[m [masync def login(response: Response, request: Request, payload: LoginRequest, db:[m
     if is_user_locked_out(payload.username):[m
         logger.warning("Blocked login attempt for locked account: %r", payload.username)[m
         raise HTTPException([m
[31m-            status_code=status.HTTP_403_FORBIDDEN,[m
[32m+[m[32m            status_code=status.HTTP_423_LOCKED,[m
             detail=f"Tài khoản bị tạm khóa do nhập sai nhiều lần. Vui lòng thử lại sau {settings.auth_lockout_minutes} phút."[m
         )[m
 [m
[36m@@ -266,28 +288,21 @@[m [masync def login(response: Response, request: Request, payload: LoginRequest, db:[m
 [m
     # Generate CSRF token[m
     csrf_token = secrets.token_urlsafe(32) # Generate a random string[m
[31m-[m
[31m-    # Set HttpOnly access_token cookie[m
[31m-    response.set_cookie([m
[31m-        key="access_token",[m
[31m-        value=f"Bearer {token}",[m
[31m-        httponly=True,[m
[31m-        max_age=expires_in,[m
[31m-        samesite="lax", # Or "strict" for higher security, but might break some legitimate cross-site navigation[m
[31m-        secure=settings.environment == "production", # Use True in production with HTTPS[m
[31m-        path="/"[m
[31m-    )[m
[31m-[m
[31m-    # Set JS-readable CSRF token cookie[m
[32m+[m[32m    # Gửi CSRF token về cookie để frontend có thể đọc và gửi lại trong Header[m
     response.set_cookie([m
         key="csrf_token",[m
         value=csrf_token,[m
[31m-        max_age=expires_in,[m
[32m+[m[32m        httponly=False,  # Quan trọng: Phải để False để JavaScript (React) có thể đọc được[m
         samesite="lax",[m
         secure=settings.environment == "production",[m
         path="/"[m
     )[m
 [m
[32m+[m[32m    # Note: we intentionally do not set cookies here. The access token is[m
[32m+[m[32m    # returned in the response body so tests and non-browser clients can use[m
[32m+[m[32m    # it via the Authorization header. Cookie-based storage can be added by[m
[32m+[m[32m    # the frontend if desired.[m
[32m+[m
     logger.info("User %r logged in successfully", user.username)[m
     record_audit(db, user.username, "login", client_ip=get_client_ip(request))[m
 [m
[36m@@ -391,7 +406,7 @@[m [masync def create_user([m
     if UserRepository.get_by_email(db, user_data.email):[m
         raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="Email đã tồn tại")[m
 [m
[31m-    new_user = UserRepository.create(db, user_data.dict())[m
[32m+[m[32m    new_user = UserRepository.create(db, user_data.model_dump())[m
     return UserInfo(id=new_user.id, username=new_user.username, email=new_user.email, role=new_user.role)[m
 [m
 [m
[1mdiff --git a/backend/api/middleware/rate_limit.py b/backend/api/middleware/rate_limit.py[m
[1mindex f470a8a..2b214b8 100644[m
[1m--- a/backend/api/middleware/rate_limit.py[m
[1m+++ b/backend/api/middleware/rate_limit.py[m
[36m@@ -24,8 +24,8 @@[m [mfrom starlette.responses import JSONResponse, Response[m
 [m
 # (max_requests, window_seconds)[m
 _ROUTE_LIMITS: list[Tuple[str, int, int]] = [[m
[31m-    ("/api/sniffer/",    60, 60),[m
[31m-    ("/api/whitelist/",  60, 60),[m
[32m+[m[32m    ("/api/sniffer/",    10, 60),[m
[32m+[m